Wild
	start default on load
	join default if spawned in Forest
HeroesGuild
	start default on load
	join default if host is a *.Hero
	joined {
	  recruit join Party(recruit->name + "\'s Party") null
	  recruit gains Swing
	  recruit equip stick
	}
Party
Hive
Pack
Cubs